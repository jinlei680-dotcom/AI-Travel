version: "3.9"
services:
  ai-travel-web:
    build: .
    image: ai-travel-web:latest
    ports:
      - "3000:3000"
    env_file:
      - ./.env
    environment:
      - PORT=3000
    restart: unless-stopped
